export class ApiConfig {
  static baseUrl = "http://127.0.0.1:8000/";
  static connectTimeout = 100 * 1000; // en millisecondes (100 secondes)
  static receiveTimeout = 50 * 1000;  // en millisecondes (50 secondes)
}
